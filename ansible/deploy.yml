#- hosts: localhost
#  connection: local
#  tasks:
#
#    - name: Stop old container
#      shell: docker stop ticket-container
#      ignore_errors: yes
#
#    - name: Remove old container
#      shell: docker rm ticket-container
#      ignore_errors: yes
#
#    - name: Pull Docker Image
#      shell: "docker pull blanck21/ticket-reservation:{{ image_tag }}"
#
#    - name: Run Container
#      shell: "docker run -d -p 2080:8080 --name ticket-container blanck21/ticket-reservation:{{ image_tag }}"
#
#

- hosts: localhost
  connection: local
  vars:
    render_service_id: "srv-d74e5k6uk2gs73a3g6m0"
    render_api_key: "{{ lookup('env', 'RENDER_API_KEY') }}"

  tasks:

    - name: Trigger deployment on Render
      shell: |
        curl -X POST "https://api.render.com/v1/services/{{ render_service_id }}/deploys" \
        -H "Authorization: Bearer {{ render_api_key }}" \
        -H "Content-Type: application/json"